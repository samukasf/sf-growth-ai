function renderVision(v){v.innerHTML=`<h1>Meu Futuro</h1><p class="muted">Mapa dos sonhos ligado a objetivos reais.</p><div class="card"><div class="form"><label class="field">Pilar<select id="visP">${PKEYS.map(p=>`<option value="${p}">${PLABEL[p]}</option>`).join('')}</select></label><label class="field">Título<input id="visT" placeholder="Ex.: casa com espaço para a família"></label></div><label class="field" style="margin-top:10px">Por que isto importa?<textarea id="visN" rows="3"></textarea></label><button class="btn primary" style="margin-top:12px" onclick="addVision()">Adicionar ao mapa</button></div><div class="vision" style="margin-top:18px">${state.vision.map(x=>`<div class="vcard"><div><div class="ey">${PLABEL[x.pillar]}</div><h3>${esc(x.title)}</h3><p class="muted">${esc(x.note||'')}</p></div></div>`).join('')}</div>`}async function addVision(){const t=$id('visT').value.trim();if(!t)return;await sb.from('future_me_vision_items').insert({user_id:user.id,pillar:$id('visP').value,title:t,note:$id('visN').value});await loadAll();render()}
function renderMissions(v){v.innerHTML=`<h1>Missões e Marcos</h1><div class="card"><div class="form"><label class="field">Pilar<select id="misP">${PKEYS.map(p=>`<option value="${p}">${PLABEL[p]}</option>`).join('')}</select></label><label class="field">Missão<input id="misT" placeholder="Ex.: caminhar 30 minutos"></label></div><button class="btn primary" style="margin-top:12px" onclick="addMission()">Criar missão</button></div><div class="card" style="margin-top:18px">${state.missions.map(x=>`<div class="row"><span class="ey">+${x.xp} XP · ${PLABEL[x.pillar]}</span><br><b>${esc(x.title)}</b>${x.status!=='done'?`<button class="btn" style="float:right" onclick="doneMission('${x.id}',${x.xp})">Concluir</button>`:'<span class="ok" style="float:right">Concluída</span>'}</div>`).join('')||'<p class="muted">Crie a primeira missão.</p>'}</div>`}async function addMission(){const t=$id('misT').value.trim();if(!t)return;await sb.from('future_me_missions').insert({user_id:user.id,pillar:$id('misP').value,title:t,xp:20});await loadAll();render()}async function doneMission(id,xp){await sb.from('future_me_missions').update({status:'done',completed_at:new Date().toISOString()}).eq('id',id);await sb.from('future_me_profiles').update({xp:(profile.xp||0)+xp}).eq('user_id',user.id);await loadAll();render()}
function renderDeclarations(v){v.innerHTML=`<h1>Declarações</h1><p class="muted">Identidade, gratidão e comportamento — sem garantia de causalidade.</p><div class="card"><div class="form"><label class="field">Pilar<select id="decP">${PKEYS.map(p=>`<option value="${p}">${PLABEL[p]}</option>`).join('')}</select></label><label class="field">Declaração<textarea id="decT" rows="3" placeholder="Eu tomo decisões coerentes com a liberdade que estou a construir."></textarea></label></div><button class="btn primary" style="margin-top:12px" onclick="addDeclaration()">Guardar</button></div><div class="grid4">${state.declarations.map(x=>`<div class="card"><div class="ey">${PLABEL[x.pillar]||'Geral'}</div><p>“${esc(x.text)}”</p></div>`).join('')}</div>`}async function addDeclaration(){const t=$id('decT').value.trim();if(!t)return;await sb.from('future_me_declarations').insert({user_id:user.id,pillar:$id('decP').value,text:t,category:'identity'});await loadAll();render()}
async function renderLetter(v){const{data:l}=await sb.from('future_me_letters').select('*').eq('user_id',user.id).maybeSingle();const txt=l?.content||`Estou a escrever da vida que decidiu construir. A diferença não veio de pensar positivo por si só, mas de alinhar decisões, hábitos e prioridades com aquilo que declarou importante. A sua visão era: ${profile?.vision_text||'construir uma vida mais alinhada'}. Continue a fazer hoje aquilo que o seu futuro agradecerá.`;v.innerHTML=`<h1>Carta do Eu Futuro</h1><div class="card"><label class="field">Carta<textarea id="letterText" rows="14">${esc(txt)}</textarea></label><button class="btn primary" style="margin-top:12px" onclick="saveLetter()">Guardar carta</button></div>`}async function saveLetter(){const{error}=await sb.from('future_me_letters').upsert({user_id:user.id,horizon_years:profile.horizon_years||5,content:$id('letterText').value},{onConflict:'user_id'});if(error)return alert(error.message);alert('Carta guardada.')}
function renderProfile(v){v.innerHTML=`<h1>Perfil e Visão</h1><p class="muted">Defina a direção que o sistema deve usar para avaliar escolhas.</p><div class="card"><div class="form"><label class="field">Nome<input id="pr_name" value="${esc(profile.display_name||'')}"></label><label class="field">País<input id="pr_country" value="${esc(profile.country||'')}"></label><label class="field">Horizonte<select id="pr_horizon">${[1,3,5,10].map(x=>`<option value="${x}" ${profile.horizon_years==x?'selected':''}>${x} ano${x>1?'s':''}</option>`).join('')}</select></label><label class="field">Idioma<select id="pr_lang"><option value="pt">Português</option><option value="en" ${profile.language==='en'?'selected':''}>English</option></select></label></div><label class="field" style="margin-top:12px">Como é a vida que quer construir?<textarea id="pr_vision" rows="5">${esc(profile.vision_text||'')}</textarea></label><div class="form" style="margin-top:12px"><label class="field">Obstáculos<textarea id="pr_obs" rows="4">${esc(profile.obstacles||'')}</textarea></label><label class="field">Recursos atuais<textarea id="pr_res" rows="4">${esc(profile.resources||'')}</textarea></label></div><label class="field" style="margin-top:12px">Primeiro compromisso<input id="pr_commit" value="${esc(profile.first_commitment||'')}"></label><label style="display:flex;gap:8px;align-items:center;margin-top:12px"><input id="pr_consent" type="checkbox" ${profile.consent_sensitive?'checked':''}> Autorizo guardar dados opcionais de bem-estar para personalização.</label><div class="actions" style="margin-top:14px"><button class="btn primary" onclick="saveProfile()">Guardar perfil</button><button class="btn danger" onclick="deleteAccount()">Eliminar meus dados</button></div><p class="muted small">XP atual: ${profile.xp||0}</p></div>`}async function saveProfile(){const row={user_id:user.id,display_name:$id('pr_name').value,country:$id('pr_country').value,horizon_years:+$id('pr_horizon').value,language:$id('pr_lang').value,vision_text:$id('pr_vision').value,obstacles:$id('pr_obs').value,resources:$id('pr_res').value,first_commitment:$id('pr_commit').value,consent_sensitive:$id('pr_consent').checked,onboarding_completed:true};const{error}=await sb.from('future_me_profiles').upsert(row);if(error)return alert(error.message);await loadAll();$id('who').textContent=`Olá, ${profile.display_name||user.email}`;alert('Perfil guardado.')}async function deleteAccount(){if(!confirm('Eliminar os dados do Future Me desta conta? Esta ação não elimina automaticamente a conta de autenticação.'))return;for(const t of ['future_me_mentor_messages','future_me_missions','future_me_declarations','future_me_vision_items','future_me_weekly_reviews','future_me_checkins','future_me_actions','future_me_goals','future_me_pillar_profiles','future_me_score_snapshots','future_me_letters','future_me_profiles'])await sb.from(t).delete().eq('user_id',user.id);await logout()}
function renderReview(v){const s=state.scores,planned=state.actions.length,completed=state.actions.filter(x=>x.status==='done').length,obs=state.checkins.find(x=>x.obstacle)?.obstacle;v.innerHTML=`<h1>Revisão Semanal</h1><div class="grid4"><div class="card"><div class="muted">Planeado</div><div class="price">${planned}</div></div><div class="card"><div class="muted">Concluído</div><div class="price">${completed}</div></div><div class="card"><div class="muted">Execução</div><div class="price">${s.execution}%</div></div><div class="card"><div class="muted">Gargalo</div><div class="price" style="font-size:24px">${PLABEL[s.bottleneck]}</div></div></div><div class="card" style="margin-top:18px"><h3>Leitura automática</h3><p class="muted">${obs?`Obstáculo recente: “${esc(obs)}”. `:''}O pilar com menor evidência de progresso é ${PLABEL[s.bottleneck]}. Reduza compromissos e escolha uma ação repetível para a próxima semana.</p></div>`}
function renderPlans(v){v.innerHTML=`<h1>Planos</h1><p class="muted">Checkout ainda em beta; nenhum pagamento será cobrado nesta versão de validação.</p><div class="pricegrid"><div class="card"><h3>Starter</h3><div class="price">€4,99</div><p>Vision Board, carta, declarações e plano inicial.</p><button class="btn" disabled>Checkout em breve</button></div><div class="card"><h3>Complete</h3><div class="price">€14,99</div><p>Plano completo e futuras visualizações.</p><button class="btn" disabled>Checkout em breve</button></div><div class="card"><h3>Future Me+</h3><div class="price">€4,99/mês</div><p>Mentor e acompanhamento contínuo.</p><button class="btn" disabled>Checkout em breve</button></div></div>`}
async function renderAdmin(v){if(!(role==='admin'||role==='superadmin')){v.innerHTML='<div class="card"><h1>Acesso restrito</h1></div>';return}const [u,g,c,s]=await Promise.all([sb.from('future_me_profiles').select('*',{count:'exact',head:true}),sb.from('future_me_goals').select('*',{count:'exact',head:true}),sb.from('future_me_checkins').select('*',{count:'exact',head:true}),sb.from('future_me_score_snapshots').select('overall').limit(1000)]);const avg=s.data?.length?Math.round(s.data.reduce((a,x)=>a+(x.overall||0),0)/s.data.length):0;v.innerHTML=`<h1>Superadmin</h1><p class="muted">Métricas agregadas; não mostramos conteúdo íntimo dos check-ins.</p><div class="grid4"><div class="card"><div class="muted">Utilizadores</div><div class="price">${u.count||0}</div></div><div class="card"><div class="muted">Metas</div><div class="price">${g.count||0}</div></div><div class="card"><div class="muted">Check-ins</div><div class="price">${c.count||0}</div></div><div class="card"><div class="muted">Score médio</div><div class="price">${avg}</div></div></div>`}
(async()=>{const{data}=await sb.auth.getSession();if(data.session)await boot(data.session);sb.auth.onAuthStateChange((ev,s)=>{if(ev==='SIGNED_IN'&&s&&!session)boot(s);if(ev==='SIGNED_OUT')logout()})})();
