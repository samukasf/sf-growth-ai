export type {
  CompanyId,
  DomainEventId,
  KnowledgeInsightId,
  KnowledgeRecordId,
  KnowledgeRelationId,
} from "./identifiers";

export { clampScore, MAX_SCORE, MIN_SCORE, type Score } from "./scores";

export { err, ok, type Result } from "./result";
