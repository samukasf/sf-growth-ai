export interface BusinessCommunicationPort {
  isAvailable(): boolean;
}

export interface BusinessAutomationPort {
  isAvailable(): boolean;
}

export interface ExecutiveCrmPort {
  isAvailable(): boolean;
}

export interface ExecutiveOrchestratorPort {
  isAvailable(): boolean;
}

export interface CompanyBrainPort {
  isAvailable(): boolean;
}

export interface OrganizationBrainPort {
  isAvailable(): boolean;
}

export interface ExecutiveOperationsPort {
  isAvailable(): boolean;
}

export interface ExecutiveHRPort {
  isAvailable(): boolean;
}
