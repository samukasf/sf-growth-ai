// Compatibility endpoint for older Google OAuth client configurations.
// The active OAuth flow uses /api/integrations/google/oauth/callback.
export { dynamic, GET } from "../../google/oauth/callback/route";
